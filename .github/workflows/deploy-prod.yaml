name: Deploy to PROD

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy (e.g., test_2026-02-03_073053)'
        required: true
        type: string

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: swissfederalarchives/lindas-admin-ch

jobs:
  deploy:
    name: Deploy to PROD
    runs-on: ubuntu-latest
    environment: production

    permissions:
      packages: write

    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Save current as previous
        run: |
          if docker pull $REGISTRY/$IMAGE_NAME:graphdb-prod 2>/dev/null; then
            echo "Saving current 'graphdb-prod' as 'graphdb-prod-previous'"
            docker tag $REGISTRY/$IMAGE_NAME:graphdb-prod $REGISTRY/$IMAGE_NAME:graphdb-prod-previous
            docker push $REGISTRY/$IMAGE_NAME:graphdb-prod-previous
          else
            echo "No existing 'graphdb-prod' tag to save"
          fi

      - name: Pull version image
        run: |
          echo "Pulling $REGISTRY/$IMAGE_NAME:${{ github.event.inputs.version }}"
          docker pull $REGISTRY/$IMAGE_NAME:${{ github.event.inputs.version }}

      - name: Tag as graphdb-prod
        run: |
          docker tag $REGISTRY/$IMAGE_NAME:${{ github.event.inputs.version }} $REGISTRY/$IMAGE_NAME:graphdb-prod
          docker push $REGISTRY/$IMAGE_NAME:graphdb-prod
          echo "Tagged ${{ github.event.inputs.version }} as graphdb-prod"

      - name: Summary
        run: |
          echo "## Deployed to PROD" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tag | Version |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| \`graphdb-prod\` | \`${{ github.event.inputs.version }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| \`graphdb-prod-previous\` | (previous version) |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "To rollback: Run **Rollback PROD** workflow" >> $GITHUB_STEP_SUMMARY
